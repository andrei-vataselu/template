# Copy to dev.hcl and replace ACCOUNT_ID
# Then: cd infra/environments/dev && terraform init -backend-config=../../backends/dev.hcl -migrate-state
bucket       = "popo-tfstate-ACCOUNT_ID"
key          = "dev/terraform.tfstate"
region       = "eu-west-1"
encrypt      = true
use_lockfile = true
